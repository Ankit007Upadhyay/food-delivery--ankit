{
  "name": "go-restro-standalone",
  "version": "1.0.0",
  "description": "A complete standalone food delivery system",
  "main": "index.js",
  "scripts": {
    "setup": "cd backend && node setupDatabase.js",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd frontend && npm run dev",
    "start:admin": "cd admin && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install && cd ../admin && npm install",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:admin\""
  },
  "keywords": [
    "food-delivery",
    "react",
    "nodejs",
    "mongodb",
    "express",
    "standalone"
  ],
  "author": "GoRestro Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
